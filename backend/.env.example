DATABASE_URL=postgresql://postgres:password@localhost:5432/personal_library

# Optional: comma-separated frontend origins allowed by CORS
# CORS_ORIGINS=http://localhost:5173,http://localhost:5174

# Optional: AI recommendations from Claude (needs credits at https://console.anthropic.com).
# Leave unset to use the free Open Library-based recommender.
# ANTHROPIC_API_KEY=
